[project]
name = "proxy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]",
    "websockets",
    "uvicorn",
    "httpx",
    "pydantic",
]

[tool.ruff]
line-length = 125